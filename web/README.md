# Buzz web

## Compiler commands

`pnpm run typecheck` runs native TypeScript 7. Next and the page-rendering test
fixture use the real TypeScript 6 compatibility API installed as `typescript`.
React remains on 19.2 while React Three Fiber excludes 19.3.
